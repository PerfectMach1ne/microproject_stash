#! tclsh

catch {console show};

proc status {} {

};

proc add {} {

};

proc rem {} {

};

proc commit {} {

};

proc uncommit {} {

}

proc push {} {

}

proc head {} {

}; # "head change", as in the latest OFFICIAL change PUSHED to the website.

proc hat {} {

}; # "hat change", as in the latest change COMMITTED and WAITING to be published.

puts {test test, testo mikrofone!!!!};

flush stdout;
set c [gets stdin];
while {$c != "q"} {
  #
  flush stdout;
  set c [gets stdin]
};